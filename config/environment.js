'use strict';

module.exports = function(/* environment, appConfig */) {
  return {
    "libpixel": {
      host: "test.libpx.com",
      domain: "test",
      defaultSource: "ember-libpixel",
      https: false
    }
  };
};
